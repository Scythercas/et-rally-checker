# CLAUDE.md

このファイルは、Claude Codeが本プロジェクトで作業する際に毎回自動的に読み込む「常駐メモ」です。
ここに書かれたルールは、会話が長くなっても・要約されても失われません。作業を始める前に必ず内容を守ってください。

## プロジェクト概要

ETロボコン2026の「ETラリー」向け、走行シミュレータのブラウザアプリ。

- インターネット非接続、ローカルのHTML/CSS/JSのみで動作（サーバ・外部ライブラリ不可が前提）。
- 走行体の代わりに `Packman.png`、走行ルートは `PackmanCookie.png`（クッキー）で可視化する。
- 詳細仕様は必ず **最新版の設計書** [docs/設計書_v002.md](docs/設計書_v002.md) を正とする。本ファイルは要約であり、矛盾があれば設計書側が正しい。旧版（`設計書_v001.md`）は履歴として残しているだけなので参照しないこと。
- **現状のプロジェクト段階**: 実装済み。`index.html` / `style.css` / `script.js` の3ファイルで動作する（2026-08-08）。

### 設計書の要点（トークン節約用の早見表。詳細・例外は必ず最新版設計書の本文を参照）

- コースは L/R の2種類。Rコースは Lコースの完全な左右反転。
- コースには「座標表示版」と「非表示版」の2種類が存在する。
- グリッドは列 A〜K・行 1〜11（設計書 §3 座標系）。「偶数の座標」= 列 B/D/F/H/J・行 2/4/6/8/10。
- ゲートは赤・黄・青の3種類のみ。足(2点)は必ず「偶数×偶数」座標のグレーの丸の上、隣接する座標間に置かれる。
  - 赤: 横向き固定、初期位置 左足H4・右足J4
  - 黄: 横向き固定、初期位置 左足F2・右足H2
  - 青: 縦向き固定、初期位置 上足F4・下足F6
  - パックマンの既定の開始位置は I1・左向き。この初期配置でサンプルの走行指示が完走する（2026-08-08 ユーザ指定）。
- コース画像上、列A/K沿い(3,5,7,9行目)にある青・赤・黄・緑の丸は、上記3ゲートとは別物。実機コース固有の固定マーカーであり、**本シミュレータでは無視してよい**（ユーザ確認済み。2026-08-08）。Packmanの開始位置設定機能とは無関係。
- Packmanは上下左右4方位、1マス単位の直進、左右90度回転（滑らかなアニメーション）、通過マスのクッキーを1個削除。
- コース画像内の細かいQRコード風アイコンは実機コースの模様（固定背景）であり、シミュレータが動的に着脱する「クッキー」（`PackmanCookie.png`）とは別物。
- 走行指示のテキスト形式: 1行1指示。「左」「右」「直進N」のみ。空行が指示の終端。
  - 上記以外の行のうち「円弧」から始まるものは専用メッセージ（未対応の旨）、それ以外の不正な行（不明な行、直進Nが0以下・非整数等）は「走行指示が不適切」の旨をユーザに通知する（2026-08-08 追記・設計書に明記済み）。
- 状態遷移: シミュレーション開始前 → 実行中 → 終了後。
  - 「シミュレーションを開始」で開始前→実行中へ遷移（走行指示からルートを推測しクッキーを配置）。
  - 「シミュレーションを初期化」＝実行中・終了後からの強制リセット機能。**開始前の状態に戻り、ゲート・走行体・走行指示は実行直前の状態を保持する**（ハードコードされた初期位置には戻さない）。設計書の「機能」章は当初「実行中に遷移」「初期位置に移動」という誤記があったが、2026-08-08にユーザ確認の上、設計書側を修正済み。

## 作業ルール（必ず遵守）

1. **指示の記録**: ユーザから新しい指示を受けたら、作業に着手する前後どこかで [docs/指示履歴.md](docs/指示履歴.md) の末尾に時系列で追記する。既存の記録は編集・削除しない。要約でよいが、指示の意図が分かる粒度で書く。
2. **不明点は必ず確認**: 仕様・命名・格納先など判断に迷う点は、推測で進めずAskUserQuestion等でユーザに確認する。特に設計書に明記がない事項は要確認。
3. **作業後の確認を徹底**: 作業が完了したら、変更内容を自分で見直す・妥当性を確認してからユーザに報告する（実装フェーズに入ったら、可能な範囲で実際に動かして確認する）。
4. **本ルール自体の永続化**: このCLAUDE.mdは自動読込されるため、会話がどれだけ長くなっても本ルールを見失わないこと。ルール自体に変更があった場合はこのファイルを更新する。

## ファイル構成・命名規則（2026-08-08 確定）

- **命名規則**: ファイル名は英語に統一する。設計書_vNNN.md・指示履歴.md・CLAUDE.md 等のドキュメント類は例外として日本語名のまま維持する。
- **設計書のバージョニング**: `設計書_v001.md` のように3桁ゼロ埋めの連番とする。改訂時は旧版を残したまま新しい番号のファイルを作成し、末尾の「改訂履歴」に変更内容を追記する。CLAUDE.md内の参照も最新版に更新すること。
- **フォルダ構成**: 用途別にサブフォルダへ分割する。
  - `images/` — 画像アセット一式
  - `docs/` — 設計書・指示履歴などのドキュメント
  - `samples/` — サンプルデータ（走行指示テキスト等）
  - `tests/` — 自動テスト
  - `trash/` — 未整理・破棄予定ファイルの一時置き場（**現状、意図的に手を加えていない**。後述）
  - 将来 `index.html` / `style.css` / `script.js` 等の実装ファイルはルート直下に置く想定（未着手）。

現状のアセット一覧:

| ファイル | 内容 |
| --- | --- |
| `images/Packman.png` | パックマン本体（右向き基準） |
| `images/PackmanCookie.png` | クッキー（走行ルート表示用） |
| `images/GateRed.png` / `GateYellow.png` / `GateBlue.png` | ゲート3種（赤・黄は横向き、青は縦向き） |
| `images/LCourse.png` | コース背景（Lコース・座標非表示版） |
| `images/LCourse_Coord.png` / `RCourse_Coord.png` | コース背景（座標表示版。旧名: Lコース_座標表示版.png / Rコース_座標表示版.png。設計書内の参照名もこの英語名に更新済み） |
| `images/GateLayoutSample.png` | ゲート配置の参考サンプル画像（旧名: ゲート配置サンプル.png） |
| `samples/InstructionsSample.txt` | 走行指示テキストのサンプル（旧名: 走行指示サンプル.txt） |
| `docs/設計書_v002.md` | 仕様の正本（**最新版**） |
| `docs/設計書_v001.md` | 設計書の旧版（履歴として保持。参照しない） |
| `index.html` / `style.css` / `script.js` | アプリ本体（実装） |
| `tests/test_logic.js` | ロジックの自動テスト（`node tests/test_logic.js`） |
| `docs/指示履歴.md` | ユーザ指示の時系列ログ |

### trash/ の扱いについて（ユーザ確認済み・現状維持）

- `trash/RCourse.png`: 座標非表示版Rコース画像。設計書上、座標非表示版のコース切り替えは「画像の左右反転」で実現するため、独立したRコース画像は**不要**（2026-08-08 ユーザ確認済み）。実装時も `images/LCourse.png` を反転させて使う想定で、このファイルは使わない。
- `trash/図準備用.xlsx`: 走行指示テキスト→実機制御コード・グリッド座標を変換するための作業用Excelツール。**ユーザの指示によりtrashのまま保持**（削除・移動はしない）。

## 実装のメモ

- 画面は「ステージ（正方形）> コース画像（contain配置）> マス目領域(`#grid-box`) > 各オブジェクト」の入れ子。
  オブジェクトはすべてマス目領域に対する**百分率**で配置しているため、画面サイズが変わっても位置がずれない。
- コース画像ごとのマス目領域の実測値は `script.js` の `COURSE_CAL`、ゲートの足の位置は `GATE_DEF` にある（根拠は設計書 §9.2・§9.3）。
  **これらの数値は画像から実測したもの。画像を差し替えたら必ず測り直すこと。**
- `Packman.png` は右向きが基準（回転0度）。向きと表示角度の対応は `dirToAngleBase()` に集約している。
- 動作確認の方法は下記「確認の手順」を参照。

## 確認の手順（実装を変更したら必ず実施）

1. `node --check script.js` で構文を確認する。
2. `node tests/test_logic.js` を実行する（最小限のDOMスタブ上で `script.js` を読み込み、走行指示の解析・ルート算出・衝突・ゲート通過判定・回転を検証する。全て通れば `0 failed` と表示される）。
3. ヘッドレスEdgeで実際に描画・実行させ、スクリーンショットで目視確認する。
   `"C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe" --headless=new --disable-gpu --screenshot=<出力先> --window-size=1180,1000 --virtual-time-budget=60000 file:///<index.htmlの絶対パス>`
   `--virtual-time-budget` を大きくするとタイマーが早送りされ、シミュレーション完了後の状態を撮影できる。

## 参照ドキュメント

- [docs/設計書_v002.md](docs/設計書_v002.md) — 仕様の正本（最新版）
- [docs/指示履歴.md](docs/指示履歴.md) — ユーザ指示の時系列ログ
